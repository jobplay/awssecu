services:
  cbt:
    build: .
    container_name: aws-security-cbt
    ports:
      - "${CBT_PORT:-3040}:3000"
    volumes:
      - cbt-data:/app/storage
    restart: unless-stopped

volumes:
  cbt-data:
